Small pill tag for status/labels — plain sans by default, or mono uppercase for a "ledger/commit" feel.

```jsx
<Badge tone="signal" mono>Band 1</Badge>
```

Tones: `neutral` (slate surface), `signal` (cyan-tinted, for highlighted/active state), `outline` (transparent, bordered).
