export interface DividerProps {
  /** Optional mono uppercase label centered on the rule */
  label?: string;
}
/**
 * @startingPoint section="Core" subtitle="Hairline rule, plain or labeled" viewport="700x100"
 */
export function Divider(props: DividerProps): JSX.Element;
