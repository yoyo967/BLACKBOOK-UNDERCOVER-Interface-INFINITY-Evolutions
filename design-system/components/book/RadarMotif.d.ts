export interface RadarMotifProps {
  /** Diameter in px of the outermost ring */
  size?: number;
  /** Animate the center point with a soft pulse */
  pulse?: boolean;
}
/**
 * @startingPoint section="Book" subtitle="Concentric cyan-ring 'all-seeing eye' leitmotif" viewport="700x260"
 */
export function RadarMotif(props: RadarMotifProps): JSX.Element;
